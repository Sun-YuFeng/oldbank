# 前端未接入接口清单

## 📋 概述

根据API文档对比前端代码，以下是**未接入**或**路径不一致**的接口：

---

## ❌ 完全未接入的接口（5个）

### 1. **仪表板概览数据接口**
- **文档接口**: `GET /api/dashboard/overview`
- **前端使用**: `GET /api/dashboard/stats`（路径不一致）
- **功能**: 获取仪表板概览数据（总用户数、总志愿者数、总任务数等）
- **状态**: ⚠️ 路径不一致，需要确认后端实际接口路径

### 2. **管理员账户列表接口**
- **文档接口**: `GET /api/accounts`
- **前端使用**: `GET /api/admin/auth/accounts`（路径不一致）
- **功能**: 获取管理员账户列表（分页）
- **状态**: ⚠️ 路径不一致，前端使用的是认证相关接口

### 3. **创建管理员账户接口**
- **文档接口**: `POST /api/accounts`
- **前端状态**: ❌ 未找到调用
- **功能**: 创建新的管理员账户
- **使用场景**: 账号管理页面需要此接口来创建新管理员

### 4. **文件上传接口**
- **文档接口**: `POST /api/files/upload`
- **前端状态**: ❌ 未找到调用
- **功能**: 上传文件（图片、文档等）
- **使用场景**: 投诉管理页面可能需要上传投诉图片

### 5. **志愿者列表接口（标准路径）**
- **文档接口**: `GET /api/admin/volunteers`
- **前端使用**: `GET /api/volunteers`（路径不一致）
- **功能**: 获取志愿者列表（分页、搜索）
- **状态**: ⚠️ 路径不一致

### 6. **志愿者详情接口（标准路径）**
- **文档接口**: `GET /api/admin/volunteers/{id}`
- **前端使用**: `GET /api/volunteers/{id}`（路径不一致）
- **功能**: 获取志愿者详细信息
- **状态**: ⚠️ 路径不一致

---

## ⚠️ 路径不一致的接口（需要确认）

### 1. **管理员登录接口**
- **文档路径**: `POST /api/auth/admin/login`
- **前端路径**: `POST /api/admin/auth/login`
- **状态**: ⚠️ 路径顺序不同，需要确认后端实际路径

### 2. **仪表板统计接口**
- **文档路径**: `GET /api/dashboard/overview`
- **前端路径**: `GET /api/dashboard/stats`
- **状态**: ⚠️ 路径不同，需要确认后端实际路径

### 3. **管理员账户列表接口**
- **文档路径**: `GET /api/accounts`
- **前端路径**: `GET /api/admin/auth/accounts`
- **状态**: ⚠️ 路径不同，需要确认后端实际路径

### 4. **志愿者相关接口**
- **文档路径**: `/api/admin/volunteers/*`
- **前端路径**: `/api/volunteers/*`
- **状态**: ⚠️ 缺少 `/admin` 前缀，需要确认后端实际路径

---

## ✅ 已接入的接口（19个）

以下接口已在前端代码中实现：

1. ✅ `GET /api/users` - 获取用户列表
2. ✅ `GET /api/users/count` - 获取用户总数
3. ✅ `GET /api/users/creation-stats` - 获取创号统计
4. ✅ `GET /api/users/role-stats` - 获取角色分布统计
5. ✅ `GET /api/users/status-stats` - 获取状态分布统计
6. ✅ `POST /api/users/ban` - 禁用/解禁用户
7. ✅ `DELETE /api/users/{userId}` - 删除用户
8. ✅ `GET /api/admin/demands` - 获取任务列表
9. ✅ `GET /api/admin/demands/{id}` - 获取任务详情
10. ✅ `GET /api/complaints` - 获取投诉列表
11. ✅ `GET /api/complaints/{id}` - 获取投诉详情
12. ✅ `POST /api/complaints/{id}/handle` - 处理投诉
13. ✅ `GET /api/admin/volunteers/ranking` - 获取志愿者排名
14. ✅ `GET /api/admin/volunteers/weekly-service-volume` - 获取本周服务量趋势
15. ✅ `GET /api/admin/warm-coin-records` - 获取暖龄币记录列表
16. ✅ `GET /api/admin/warm-coin-records/{id}` - 获取记录详情
17. ✅ `GET /api/admin/warm-coin-records/user/{userId}` - 获取用户暖龄币记录
18. ✅ `POST /api/admin/auth/login` - 管理员登录（路径与文档不同）
19. ✅ `GET /api/admin/auth/*` - 认证相关接口（注册、审核等）

---

## 🔧 需要处理的问题

### 高优先级（功能缺失）

1. **创建管理员账户接口** (`POST /api/accounts`)
   - 账号管理页面需要此功能
   - 建议在 `AccountManagementPage.vue` 中添加创建账号功能

2. **文件上传接口** (`POST /api/files/upload`)
   - 投诉管理可能需要上传图片
   - 建议在 `ComplaintManagementPage.vue` 或 `ComplaintDetailPage.vue` 中添加

3. **仪表板概览接口路径确认**
   - 当前使用 `/api/dashboard/stats`
   - 文档中是 `/api/dashboard/overview`
   - 需要确认后端实际路径并统一

### 中优先级（路径确认）

1. **志愿者接口路径统一**
   - 文档: `/api/admin/volunteers/*`
   - 前端: `/api/volunteers/*`
   - 需要确认后端实际路径

2. **管理员账户接口路径统一**
   - 文档: `/api/accounts`
   - 前端: `/api/admin/auth/accounts`
   - 需要确认后端实际路径

---

## 📝 建议

1. **统一接口路径**: 建议与后端开发确认实际接口路径，统一前后端接口规范
2. **补充缺失功能**: 优先实现创建管理员账户和文件上传功能
3. **接口文档更新**: 如果后端接口路径与文档不一致，建议更新API文档

---

**生成时间**: 2025-01-XX  
**文档版本**: v1.0

